/// Static photography for the Plan page's cover banner (design provided
/// separately from Figma, see the trip feature's `TripImages` precedent).
abstract class PlanImages {
  /// `docs/design/README.md` § 5 — the banner always shows this bundled
  /// photo, independent of the trip's own `cover_image_path`.
  static const banner = 'assets/images/journal/balloons_wide.png';
}
